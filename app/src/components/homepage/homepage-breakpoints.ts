/** Matches desktop feature stack — sticky cards and scroll lock. */
export const HOMEPAGE_FEATURE_STACK_MIN_WIDTH_PX = 1024;

export const HOMEPAGE_FEATURE_STACK_MEDIA_QUERY = `(min-width: ${HOMEPAGE_FEATURE_STACK_MIN_WIDTH_PX}px)`;
