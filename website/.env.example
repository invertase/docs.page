# Bundler API (server-side), no trailing slash.
# In production / next start, unset defaults to http://localhost:8080 — set this on your host.
# In next dev, if unset, the site uses https://staging-api.docs.page so it works without api/.
# When developing api/ locally alongside Next.js:
# API_URL=http://localhost:8080
