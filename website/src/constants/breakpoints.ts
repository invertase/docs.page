/** Matches Tailwind `marketingNav` screen — burger menu vs desktop nav. */
export const MARKETING_NAV_MIN_WIDTH_PX = 813;

export const MARKETING_NAV_MEDIA_QUERY = `(min-width: ${MARKETING_NAV_MIN_WIDTH_PX}px)`;
