---
title: Tabs
description: Switchable content panels for platform or option variants.
---

Use `<Tabs>` to organize content into switchable panels. Add any number of `<TabItem>` children and include other MDX content—code blocks, callouts, or lists—inside each panel.

## Example

<Tabs>
  <TabItem label="First tab" value="first">
    Welcome to the content inside the first tab.

    You can nest other components inside tabs. For example, a code block:

    ```js
    export function greet(name) {
      return `Hello, ${name}!`;
    }
    ```
  </TabItem>
  <TabItem label="Second tab" value="second">
    Content that appears only when the second tab is selected.
  </TabItem>
  <TabItem label="Third tab" value="third">
    Content that appears only when the third tab is selected.
  </TabItem>
</Tabs>

````mdx
<Tabs>
  <TabItem label="First tab" value="first">
    Welcome to the content inside the first tab.

    You can nest other components inside tabs. For example, a code block:

    ```js
    export function greet(name) {
      return `Hello, ${name}!`;
    }
    ```
  </TabItem>
  <TabItem label="Second tab" value="second">
    Content that appears only when the second tab is selected.
  </TabItem>
  <TabItem label="Third tab" value="third">
    Content that appears only when the third tab is selected.
  </TabItem>
</Tabs>
````

## `Tabs` props

<Property name="defaultValue" type="string" optional>
  The tab to show on first load. Must match a `TabItem` `value`.

  Defaults to the first tab.
</Property>

---

<Property name="groupId" type="string" optional>
  A shared identifier for synchronized tab groups. Tabs with the same `groupId` stay in sync by matching `value` props across groups.

  When set, the selected tab is stored in local storage and restored on reload. Preview mode uses the key `preview:tabs:{groupId}`; published docs use `docs.page:tabs:{owner}/{repository}:{groupId}`.
</Property>

## `TabItem` props

<Property name="label" type="string" required>
  The text shown on the tab trigger.
</Property>

---

<Property name="value" type="string" required>
  A unique identifier for the tab within its group. Used by `defaultValue` and by `groupId` synchronization across tab groups.
</Property>

## Synchronization

When the same options appear in more than one place—for example, language-specific install steps and matching API examples—give each `<Tabs>` group the same `groupId`. Without `groupId`, each tab group on the page operates independently.

The example below uses two groups that share `groupId="language"`. Selecting **JavaScript** in either group selects **JavaScript** in both.

<Tabs groupId="language">
  <TabItem label="JavaScript" value="js">
    ```js
    import axios from 'axios';
    ```
  </TabItem>
  <TabItem label="Dart" value="dart">
    ```dart
    import 'package:dio/dio.dart';
    ```
  </TabItem>
</Tabs>

<Tabs groupId="language">
  <TabItem label="JavaScript" value="js">
    ```js
    const response = await axios.get('https://example.com');
    ```
  </TabItem>
  <TabItem label="Dart" value="dart">
    ```dart
    final response = await Dio().get('https://example.com');
    ```
  </TabItem>
</Tabs>

````mdx
<Tabs groupId="language">
  <TabItem label="JavaScript" value="js">...</TabItem>
  <TabItem label="Dart" value="dart">...</TabItem>
</Tabs>

<Tabs groupId="language">
  <TabItem label="JavaScript" value="js">...</TabItem>
  <TabItem label="Dart" value="dart">...</TabItem>
</Tabs>
````

## Behavior

| Condition | Result |
| --- | --- |
| No valid `<TabItem>` children | Renders nothing |
| Selected `value` is not in the current group | Falls back to `defaultValue`, then the first tab |
| `groupId` omitted | Tab groups on the page operate independently |

## See also

- [Components overview](/components/index) — when to use tabs, code groups, and accordions
- [Reference template](/templates/reference) — structure for lookup pages like this one
