---
title: Quickstart
description: Publish a live documentation site from a public GitHub repository in minutes.
---

This tutorial walks you through your first publish: scaffold the required files, push them to a public GitHub repository, and open your live [docs.page](https://docs.page) URL. When you finish, your documentation site is on the web — no build step or hosting setup required.

## Prerequisites

- A [GitHub](https://github.com) account
- [Git](https://git-scm.com/downloads) installed locally
- [Node.js](https://nodejs.org/) (to run the CLI via `npx`)

## Steps

<Steps>
  <Step title="Scaffold your docs files">
    Open a terminal at your project root — an empty folder or an existing repository — and run:

    ```bash
    npx @docs.page/cli init
    ```

    The CLI asks for your project name and whether to create starter pages. Accept the defaults unless you already have a `docs/` directory you want to keep.

    When init finishes, your project contains:

    ```
    docs.json
    docs/
      index.mdx
      next-steps.mdx
    ```

    - **`docs.json`** — site configuration: name, description, sidebar, and theme
    - **`docs/index.mdx`** — your home page (served at `/`)
    - **`docs/next-steps.mdx`** — a second page (served at `/next-steps`)

    Open `docs/index.mdx` and change the title or add a sentence so you can confirm your changes later on the live site.
  </Step>
  <Step title="Push to a public GitHub repository">
    [docs.page](https://docs.page) only hosts **public** repositories. Create a new public repo on GitHub (for example `acme/my-docs`), then commit and push from your project directory:

    ```bash
    git init
    git add docs.json docs/
    git commit -m "Add docs.page site"
    git branch -M main
    git remote add origin https://github.com/acme/my-docs.git
    git push -u origin main
    ```

    Replace `acme/my-docs` with your GitHub owner and repository name.

    <Warning>
      If the repository is private, [docs.page](https://docs.page) returns an error and does not serve your documentation. Make the repository public before opening your live URL.
    </Warning>
  </Step>
  <Step title="Open your live site">
    Your documentation is live the instant your push completes. Open your browser and navigate to:

    ```
    https://docs.page/{owner}/{repo}
    ```

    For example, if your repository is `https://github.com/acme/my-docs`, your live site is `https://docs.page/acme/my-docs`.

    **Congratulations — your docs site is officially published.** Pushes to your default branch update this live site automatically with zero build steps.
  </Step>
</Steps>

## Next steps

- [Write documentation](/authoring/write-documentation) — structure pages, add code examples, and use built-in components
- [Organize your navigation](/authoring/organize-your-navigation) — order sidebar groups, tabs, and page links in `docs.json`
- [Local preview](/features/local-preview) — iterate on content on your machine before pushing

## Page templates

Your site is live. When you add pages, pick a layout that matches what the reader needs — a walkthrough, a single task, lookup facts, or background on *why*.

<CardGroup cols={2}>
  <Card title="Tutorial" icon="list-ol" href="/authoring/write-documentation/tutorial">
    First-time walkthrough for one thing they set out to do.
  </Card>
  <Card title="How-to" icon="wrench" href="/authoring/write-documentation/how-to">
    One specific task for readers who already know the basics.
  </Card>
  <Card title="Reference" icon="book" href="/authoring/write-documentation/reference">
    Lookup pages for APIs, config fields, commands, and exact syntax.
  </Card>
  <Card title="Explanation" icon="lightbulb" href="/authoring/write-documentation/explanation">
    Background on why something works — architecture, intent, and tradeoffs.
  </Card>
</CardGroup>
