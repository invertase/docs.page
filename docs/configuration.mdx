---
title: Configuration
description: Projects configure name, description, logo, favicon, theme colors, header, sidebar, tabs, anchors, social links, SEO, search, scripts, content rendering, and mustache variables via a docs.json file validated by Zod.
---

Projects configure name, description, logo, favicon, theme colors, header, sidebar, tabs, anchors, social links, SEO, search, scripts, content rendering, and mustache variables via a docs.json file validated by Zod.

Create a `docs.json` file at your repository root. The schema is validated with Zod and exposed at `GET /schema.json`.

## Top-level keys

| Key | Purpose |
|-----|---------|
| `name` | Site title |
| `description` | Site description |
| `logo` | Header logo (light/dark) |
| `favicon` | Browser favicon |
| `theme` | Colors and default light/dark mode — see [Theme](/theme) |
| `tabs` | Top navigation tabs — see [Navigation](/navigation) |
| `sidebar` | Page navigation — see [Navigation](/navigation) |
| `search` | Algolia DocSearch — see [Search](/search) |
| `scripts` | Analytics scripts — see [Analytics](/analytics) |
| `content` | Content rendering options |
| `variables` | Mustache variables — see [Variables](/variables) |

<Info>

Still on `docs.yaml`? See [v1 YAML migration](/v1-config-migration).

</Info>
## Implementation

- `api/src/config/schema.ts` `ConfigSchema`
- `api/src/config/index.ts` `parseConfig` — parser
