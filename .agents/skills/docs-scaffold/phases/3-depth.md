# Phase 3: Add depth

## Resuming from cold context

Read before starting:

- `.docs/scaffold-plan.md` — **approved**; implement `[depth]` leaves from **Nav** + **Merge map**
- `docs.json` from Phase 2
- `.docs/docs-spec.md`, `.docs/docs-inventory.json`
- [sidebar-ia.md](../sidebar-ia.md)
- [stub-templates.md](../stub-templates.md)

## Goal

Add **`[depth]` pages** from the nav tree (Guides, Core Concepts, Configuration, Advanced, Comparisons).

## Steps

### 3a. Extend sidebar

Add groups and leaves matching the plan **Nav** `[depth]` section. Order per plan tree.

### 3b. outline.pages + stubs

Same rules as Phase 2: `capabilityIds` from **Merge map**; metadata from topic-buckets + spec; minimal stubs only.

### 3c. Validate (light)

- ≤7 top-level groups on root tab
- No `reference` top-level in Getting Started

## CHECKPOINT

Show new groups and depth page count. Approve before Phase 4.
